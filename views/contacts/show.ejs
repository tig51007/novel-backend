<!DOCTYPE html>
<html>
  <head>
    <title>Contact</title>
    <meta charset="UTF-8">
  </head>
  <body>
    <div>Contact Book</div>
    <ul>
      <li><a href="/contacts">Index</a></li>
      <li><a href="/contacts/new">New</a></li>
    </ul>

    <div class="contact contact-show">
      <h2>Show</h2>
      <div>
        <h3><%= contact.name%></h3>
        <label>Email</label> : <span><%= contact.email %></span>
        
      </div>
      <a href="/details/<%= contact.name %> " >글 </a> <!-- 1 -->
        
      <div class="contact-menu">
        <a href="/contacts/<%= contact._id %>/edit">Edit</a> <!-- 1 -->
        <form action="/contacts/<%= contact._id %>?_method=delete" method="post"> <!-- 원래는 불가능 method-override 라이브러리 덕에 가능해짐-->
          <a href="#" onclick="confirm('Do you want to delete this?')?this.parentElement.submit():null;">Delete</a>
        </form>
      </div>
    </div>
  </body>
</html>